def hint(understanding: dict, target_description: str) -> str:
    """Return a short pedagogical prompt for the student.

    Stub: returns a canned Socratic prompt so client-side rendering can be
    validated. Real implementation calls a reasoning model constrained by a
    tutoring system prompt.
    """
    return "What else does the 3 multiply inside the parentheses?"
